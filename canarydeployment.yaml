# Canary Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-canary
  labels: 
  app: myapp  
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
      version: canary
  template:
    metadata:
      labels:
        app: 
        version: canary
    spec:
      containers:
      - name: myapp
        image: nginx:1.21:v2
